# Stylus

1. 支持选择器嵌套；
2. 分号(`;`)、逗号(`,`)和大括号(`{}`)可选
3. 父级引用(`&`)
4. 支持混入(mixin)
5. 支持变量
6. 块属性访问(`@prop`)
7. 支持for in 迭代
8. 支持插值(`{}`)
9. 支持多种操作符
10. 支持类型转换
11. 支持sprintf格式化输出
12. 支持色值运算
13. 支持函数/关键字参数